import React, { useContext } from "react"
import Layout from "../../components/layout"

const accessibility = () => {
  return (
    <Layout>
      <h1>Accessibility</h1>
    </Layout>
  )
}

export default accessibility
